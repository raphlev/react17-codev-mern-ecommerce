import classes from './LoadingSpinner.module.css';

const LoadingSpinner = () => {
  // console.log("LoadingSpinner is called ...")
  return <div className={classes.spinner}></div>;
}

export default LoadingSpinner;
